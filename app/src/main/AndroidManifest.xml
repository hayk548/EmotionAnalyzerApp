<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.emotionanalyzerapp">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.EmotionAnalyzerApp">

        <activity
            android:name=".SignupAndLogin.MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity
            android:name=".SignupAndLogin.SignUpActivity"
            android:exported="false" />
        <activity android:name=".SignupAndLogin.WaitingForEmailActivity"/>

        <activity android:name=".choose.AudioInputActivity"/>
        <activity android:name=".choose.MainActivity"/>
        <activity android:name=".choose.TextInputActivity"/>
    </application>
</manifest>